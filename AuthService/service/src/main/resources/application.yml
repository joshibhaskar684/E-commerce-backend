spring:
  kafka:
    topics:
      seller: seller-events
      shop: shop-events
      cart: cart-events
      order: order-events
      admin: admin-events
      product: product-events
      payment: payment-events
      notification: notification-events