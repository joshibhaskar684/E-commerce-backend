spring:
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: lb://AUTH-SERVICE           # Load-balanced service
          predicates:
            - Path=/auth/**                # Match all /auth requests
          filters:
            - StripPrefix=0                # Keep /auth prefix (your controller expects it)
            - AddRequestHeader=X-Forwarded-Host,localhost